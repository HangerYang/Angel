diff --git a/vllm/model_executor/models/llama_eagle3.py b/vllm/model_executor/models/llama_eagle3.py
index 2d859bd..031c4f5 100644
--- a/vllm/model_executor/models/llama_eagle3.py
+++ b/vllm/model_executor/models/llama_eagle3.py
@@ -45,9 +45,15 @@ class LlamaDecoderLayer(LlamaDecoderLayer):
         config = config or vllm_config.model_config.hf_config
         quant_config = self.get_quant_config(vllm_config)
 
-        # First layer uses 2*hidden_size (embeds + hidden_states concatenated)
-        # Subsequent layers use hidden_size (only hidden_states, no embeds)
-        qkv_input_size = 2 * self.hidden_size if layer_idx == 0 else self.hidden_size
+        self.progressive_staged = (
+            getattr(config, "eagle_aux_injection_mode", "fused_fc") == "progressive_staged"
+        )
+        # First layer (and all progressive_staged layers) use 2*hidden_size.
+        qkv_input_size = (
+            2 * self.hidden_size
+            if (layer_idx == 0 or self.progressive_staged)
+            else self.hidden_size
+        )
 
         # Parallel drafting checkpoints may have attention bias enabled
         qkv_bias = getattr(config, "attention_bias", False)
@@ -95,12 +101,23 @@ class LlamaDecoderLayer(LlamaDecoderLayer):
         embeds: torch.Tensor,
         hidden_states: torch.Tensor,
         residual: torch.Tensor | None,
+        inject: torch.Tensor | None = None,
     ) -> tuple[torch.Tensor, torch.Tensor]:
         if self.layer_idx == 0:
             # First layer: concatenate embeds with hidden_states
             embeds = self.input_layernorm(embeds)
             hidden_states, residual = self._residual_norm(hidden_states=hidden_states)
             hidden_states = torch.cat([embeds, hidden_states], dim=-1)
+        elif self.progressive_staged:
+            if inject is None:
+                raise ValueError(
+                    f"progressive_staged layer {self.layer_idx} requires inject HS"
+                )
+            # Staged inject: attn on cat(norm(h_prev), norm(aux_i)); residual on h_prev.
+            left = self.input_layernorm(hidden_states)
+            residual = hidden_states
+            right = self.hidden_norm(inject)
+            hidden_states = torch.cat([left, right], dim=-1)
         else:
             # Subsequent layers: process hidden_states and residuals only
             hidden_states, residual = self.input_layernorm(hidden_states, residual)
@@ -152,6 +169,12 @@ class LlamaModel(nn.Module):
                 "norm_before_fc", getattr(self.config, "norm_before_fc", False)
             )
         )
+        self.progressive_staged = (
+            getattr(self.config, "eagle_aux_injection_mode", "fused_fc")
+            == "progressive_staged"
+        )
+        # Per-layer inject buffers set by combine_hidden_states / set_aux_inject.
+        self._aux_inject: list[torch.Tensor] | None = None
 
         current_vllm_config = get_current_vllm_config()
 
@@ -179,41 +202,55 @@ class LlamaModel(nn.Module):
             if self.num_aux_hidden_states is None:
                 eagle_config = getattr(self.config, "eagle_config", None) or {}
                 layer_ids = eagle_config.get("eagle_aux_hidden_state_layer_ids")
-                self.num_aux_hidden_states = len(layer_ids) if layer_ids else 3
+                if layer_ids is None:
+                    layer_ids = getattr(
+                        self.config, "eagle_aux_hidden_state_layer_ids", None
+                    )
+                self.num_aux_hidden_states = (
+                    len(layer_ids)
+                    if layer_ids
+                    else (len(self.layers) if self.progressive_staged else 3)
+                )
 
             target_hidden_size = getattr(
                 self.config, "target_hidden_size", self.config.hidden_size
             )
             self.fc_input_size = target_hidden_size * self.num_aux_hidden_states
 
-            if self.norm_before_fc:
-                self.input_norm = RMSNorm(
-                    self.fc_input_size,
-                    eps=self.config.rms_norm_eps,
-                )
-            else:
+            if self.progressive_staged:
+                # No early 3H→H fusion; aux streams are injected per draft layer.
                 self.input_norm = None
-
-            use_fc_norm = getattr(self.config, "fc_norm", False)
-            if use_fc_norm:
-                self.fc_norm = nn.ModuleList(
-                    [
-                        RMSNorm(target_hidden_size, eps=self.config.rms_norm_eps)
-                        for _ in range(self.num_aux_hidden_states)
-                    ]
-                )
-            else:
                 self.fc_norm = None
-
-            self.fc = ReplicatedLinear(
-                input_size=self.fc_input_size,
-                output_size=self.config.hidden_size,
-                bias=False,
-                params_dtype=vllm_config.model_config.dtype,
-                quant_config=self.quant_config,
-                prefix=maybe_prefix(prefix, "fc"),
-                return_bias=False,
-            )
+                self.fc = None
+            else:
+                if self.norm_before_fc:
+                    self.input_norm = RMSNorm(
+                        self.fc_input_size,
+                        eps=self.config.rms_norm_eps,
+                    )
+                else:
+                    self.input_norm = None
+
+                use_fc_norm = getattr(self.config, "fc_norm", False)
+                if use_fc_norm:
+                    self.fc_norm = nn.ModuleList(
+                        [
+                            RMSNorm(target_hidden_size, eps=self.config.rms_norm_eps)
+                            for _ in range(self.num_aux_hidden_states)
+                        ]
+                    )
+                else:
+                    self.fc_norm = None
+
+                self.fc = ReplicatedLinear(
+                    input_size=self.fc_input_size,
+                    output_size=self.config.hidden_size,
+                    bias=False,
+                    params_dtype=vllm_config.model_config.dtype,
+                    quant_config=self.quant_config,
+                    prefix=maybe_prefix(prefix, "fc"),
+                    return_bias=False,
+                )
 
         self.norm_output = getattr(self.config, "norm_output", False)
         self.norm = RMSNorm(
@@ -224,6 +261,10 @@ class LlamaModel(nn.Module):
     def embed_input_ids(self, input_ids: torch.Tensor) -> torch.Tensor:
         return self.embed_tokens(input_ids)
 
+    def set_aux_inject(self, aux_inject: list[torch.Tensor] | None) -> None:
+        """Store per-layer target aux HS for progressive_staged injection."""
+        self._aux_inject = aux_inject
+
     def forward(
         self,
         input_ids: torch.Tensor,
@@ -239,12 +280,21 @@ class LlamaModel(nn.Module):
         )
 
         residual = None
-        for layer in self.layers:
+        for layer_idx, layer in enumerate(self.layers):
+            inject = None
+            if self.progressive_staged and layer_idx > 0:
+                if self._aux_inject is None or layer_idx >= len(self._aux_inject):
+                    raise ValueError(
+                        "progressive_staged forward missing aux inject "
+                        f"for layer {layer_idx}"
+                    )
+                inject = self._aux_inject[layer_idx]
             hidden_states, residual = layer(
                 positions=positions,
                 embeds=input_embeds,
                 hidden_states=hidden_states,
                 residual=residual,
+                inject=inject,
             )
         hidden_states, hidden_prenorm = self.norm(hidden_states, residual)
 
@@ -362,8 +412,22 @@ class Eagle3LlamaForCausalLM(LlamaForCausalLM):
     ) -> torch.Tensor:
         if not self.model.use_aux_hidden_state:
             return hidden_states
-        # combine multiple auxiliary hidden states returned by eagle3
 
+        if self.model.progressive_staged:
+            # hidden_states is concat of N aux streams [T, N*H]; stash per-layer
+            # injects and return the first stream as L0 residual seed.
+            n = self.model.num_aux_hidden_states
+            h = self.config.hidden_size
+            if hidden_states.shape[-1] != n * h:
+                raise ValueError(
+                    "progressive_staged combine_hidden_states expected "
+                    f"last dim {n * h}, got {hidden_states.shape[-1]}"
+                )
+            chunks = list(hidden_states.split(h, dim=-1))
+            self.model.set_aux_inject(chunks)
+            return chunks[0]
+
+        # combine multiple auxiliary hidden states returned by eagle3
         if self.model.norm_before_fc:
             hidden_states = self.model.input_norm(hidden_states)
 
@@ -378,6 +442,10 @@ class Eagle3LlamaForCausalLM(LlamaForCausalLM):
 
         return self.model.fc(hidden_states)
 
+    def set_aux_inject_from_list(self, aux_list: list[torch.Tensor]) -> None:
+        """Set progressive per-layer aux injects (e.g. after gathering last tokens)."""
+        self.model.set_aux_inject(aux_list)
+
     def load_weights(self, weights: Iterable[tuple[str, torch.Tensor]]):
         model_weights = {}
         includes_draft_id_mapping = False
diff --git a/vllm/v1/worker/gpu/spec_decode/autoregressive/speculator.py b/vllm/v1/worker/gpu/spec_decode/autoregressive/speculator.py
index 747fb3a..f75763f 100644
--- a/vllm/v1/worker/gpu/spec_decode/autoregressive/speculator.py
+++ b/vllm/v1/worker/gpu/spec_decode/autoregressive/speculator.py
@@ -174,6 +174,21 @@ class AutoRegressiveSpeculator(DraftModelSpeculator):
             hidden_states = last_hidden_states
         self.hidden_states[:num_tokens].copy_(hidden_states)
 
+        # ---------------------------------------------------------------------
+        # [ANGELSLIM EAGLE BP#2] Target → draft handoff (hidden states).
+        # Target aux/last hidden states were combined and copied into
+        # self.hidden_states; the first draft forward consumes them next.
+        # Uncomment `breakpoint()` and run eval with: --debug --pdb
+        # Inspect: hidden_states, aux_hidden_states, last_hidden_states,
+        #          self.method, self.hidden_states[:num_tokens]
+        # Skip warmup: only break when not dummy_run.
+        # File: vllm/v1/worker/gpu/spec_decode/autoregressive/speculator.py
+        #       :: EagleSpeculator/AutoRegressiveSpeculator.propose
+        # ---------------------------------------------------------------------
+        if not dummy_run:
+            # breakpoint()
+            pass
+
         self._copy_request_inputs(
             num_reqs,
             input_batch.idx_mapping,
@@ -368,6 +383,16 @@ class AutoRegressiveSpeculator(DraftModelSpeculator):
             self.hidden_states[:num_reqs] = hidden_states[last_token_indices]
         self.input_buffers.positions[:num_reqs] = positions
 
+        # Progressive staged: reuse last-token target aux for subsequent decode steps
+        # (target does not re-run between draft tokens within one propose).
+        draft_inner = getattr(self.model, "model", None)
+        if draft_inner is not None and getattr(draft_inner, "progressive_staged", False):
+            aux = getattr(draft_inner, "_aux_inject", None)
+            if aux is not None:
+                self.model.set_aux_inject_from_list(
+                    [a[last_token_indices] for a in aux]
+                )
+
     def _multi_step_decode(
         self,
         num_reqs: int,
@@ -382,6 +407,19 @@ class AutoRegressiveSpeculator(DraftModelSpeculator):
         attn_metadata = None
         slot_mappings_by_layer = None
         for step in range(1, self.num_speculative_steps):
+            # -----------------------------------------------------------------
+            # [ANGELSLIM EAGLE BP#3] Draft autoregressive loop (step >= 1).
+            # Previous draft token was written back into input_ids / hidden_states
+            # (see update_draft_inputs after _generate_draft). This iteration
+            # feeds that next token into the draft model again.
+            # Uncomment `breakpoint()` and run eval with: --debug --pdb
+            # Inspect: step, self.current_draft_step, self.draft_tokens[:num_reqs],
+            #          self.hidden_states[:num_reqs],
+            #          self.input_buffers.input_ids[:num_reqs], positions
+            # File: .../autoregressive/speculator.py :: _multi_step_decode
+            # -----------------------------------------------------------------
+            # breakpoint()
+
             # Rebuild every step when positions advance, or just once
             # on the first step when positions are constant (Gemma4 MTP).
             if not skip_attn and (self.advance_draft_positions or step == 1):
